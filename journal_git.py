def init(nickname, master_path, local_path):

    # create the bare git repo

    # create the local working copy

    # create (or add to) the .pyjournalrc file
                 
    pass


def connect(nickname, master_path, local_path):

    # git clone the bare repo at master_path into the local path

    # create (or add to) the .pyjournalrc file
    
    pass


def pull(nickname):

    pass


def push(nickname):

    pass


